# План Модернізації та Покращень (Roadmap of Improvements)

Цей документ містить пропозиції щодо технічного та функціонального покращення кожного модуля системи **AI Dungeon Master**.

---

## 1. Core Module (Ядро)
**Поточний стан:** `App.tsx` є монолітом, використовується локальний стан React (`useState`), багато "prop drilling".

### 🔧 Технічні покращення:
- [ ] **State Management:** Впровадити **Zustand** або **Redux Toolkit**. Це дозволить винести логіку з `App.tsx` у окремі слайси (store slices) та уникнути передачі пропсів на 5 рівнів вглиб.
- [ ] **Routing:** Додати **React Router**. Розділити екрани "Setup", "Lobby" та "Game" на окремі URL-адреси (наприклад, `/join/:roomId`).
- [ ] **Performance:** Впровадити `React.memo` та `useCallback` для критичних компонентів, щоб зменшити кількість ре-рендерів при оновленні чату або переміщенні токенів.

### ✨ Функціональні покращення:
- [ ] **Modding Support:** Система плагінів, що дозволяє користувачам завантажувати JSON-файли для зміни UI або правил без перекомпіляції коду.

---

## 2. AI Engine Module (Інтелект)
**Поточний стан:** Прямі запити до Gemini API, проста черга запитів, статичні системні промпти.

### 🔧 Технічні покращення:
- [ ] **RAG (Retrieval-Augmented Generation):** Інтегрувати клієнтську векторну базу даних (наприклад, `Voy` або `LangChain.js`). Замість відправки всього контексту, шукати релевантні правила D&D та факти з історії перед відправкою промпту.
- [ ] **Structured Output:** Повністю перейти на `responseSchema` (JSON schema enforcement) для всіх інструментів, щоб гарантувати валідність даних від ШІ.
- [ ] **Streaming:** Реалізувати потокову передачу тексту (Streaming API) для чату, щоб зменшити час очікування відповіді (TTFT - Time To First Token).

### ✨ Функціональні покращення:
- [ ] **Vision Analysis 2.0:** Додати можливість гравцям малювати на папері, фотографувати малюнок, і ШІ автоматично перетворюватиме це на ігрову мапу з колізіями.
- [ ] **Adaptive Difficulty:** ШІ аналізує успішність кидків гравців і динамічно коригує складність (DC) або HP ворогів "за ширмою".

---

## 3. Networking Module (Мультиплеєр)
**Поточний стан:** PeerJS, повна синхронізація стану (`STATE_SYNC`), host-authority.

### 🔧 Технічні покращення:
- [ ] **CRDT (Yjs/Automerge):** Перехід на Conflict-free Replicated Data Types для спільного редагування карти та журналу в реальному часі без конфліктів.
- [ ] **Binary Serialization:** Використання **MessagePack** або **Protobuf** замість JSON для зменшення розміру пакетів даних при передачі по WebRTC.
- [ ] **Mesh Network:** Замість чистої зірки (Host-Client), дозволити клієнтам обмінюватися даними (наприклад, позиціями курсорів) напряму для зменшення навантаження на Хоста.

### ✨ Функціональні покращення:
- [ ] **Reconnection Handler:** Більш надійний механізм відновлення сесії при короткочасній втраті інтернету ("Smooth Handover").
- [ ] **Video/Voice Chat:** Інтеграція відеозв'язку безпосередньо в додаток (використовуючи той самий WebRTC канал).

---

## 4. VTT Module (Віртуальний Стіл)
**Поточний стан:** Canvas 2D API, Raycasting на CPU, проста система частинок.

### 🔧 Технічні покращення:
- [ ] **WebGL (PixiJS / Three.js):** Міграція рендерингу карти на WebGL. Це дозволить використовувати шейдери для динамічного освітлення та обробляти тисячі частинок без лагів.
- [ ] **Virtual Scrolling:** Оптимізація списку чату та бойового трекера для відображення лише видимих елементів.

### ✨ Функціональні покращення:
- [ ] **Multi-Layer Map:** Підтримка шарів: Background, Object, GM Only (приховані пастки), Weather, Grid.
- [ ] **Dynamic Lighting 2.0:** Підтримка кольорового світла (факели = помаранчеве, магія = синє) та тіней від токенів.
- [ ] **Measurement Tools:** Шаблони для всіх заклять (Конус, Куб, Лінія) з автоматичним підсвічуванням цілей, що потрапляють у зону.

---

## 5. Character Module (Персонаж)
**Поточний стан:** Статична форма, ручний ввід багатьох параметрів.

### 🔧 Технічні покращення:
- [ ] **Form Validation:** Використання бібліотеки на кшталт `React Hook Form` + `Zod` для валідації вводу даних персонажа.

### ✨ Функціональні покращення:
- [ ] **Import/Export:** Повна підтримка імпорту персонажів з PDF-файлів або JSON-форматів популярних білдерів (D&D Beyond, 5eTools).
- [ ] **Interactive Inventory:** "Paper Doll" інтерфейс (слоти для шолома, броні, зброї) замість простого списку тексту.
- [ ] **Level Up Wizard:** Покроковий майстер підвищення рівня, що автоматично пропонує нові фічі класу та закляття.

---

## 6. Persistence & Data Module (Дані)
**Поточний стан:** Google Drive API, LocalStorage.

### 🔧 Технічні покращення:
- [ ] **IndexedDB (Dexie.js):** Використання IndexedDB замість LocalStorage для зберігання великих об'ємів даних (історії чату, зображень карт). LocalStorage має ліміт ~5-10MB.
- [ ] **Optimistic UI:** Миттєве відображення змін у UI до підтвердження збереження в хмару.

### ✨ Функціональні покращення:
- [ ] **Campaign Manager:** Можливість мати кілька окремих кампаній з різними наборами гравців та налаштувань.
- [ ] **Shared Compendium:** Можливість Хоста створювати власні довідники (монстри, предмети) і ділитися ними між різними кампаніями.

---

## 7. Audio Module (Звук)
**Поточний стан:** Web Audio API синтезатори, прості `<audio>` теги.

### 🔧 Технічні покращення:
- [ ] **Audio Sprites:** Об'єднання коротких звуків в один файл для зменшення HTTP-запитів.
- [ ] **Spatial Audio:** Реалізація позиціювання звуку. Якщо вибух стався зліва на карті, звук має лунати зліва (Stereo Panning).

### ✨ Функціональні покращення:
- [ ] **Voice Changer:** Фільтри реального часу для мікрофона (ефект демона, робота, відлуння печери) для відіграшу ролей.
- [ ] **Sound Board:** Панель для Хоста з "гарячими клавішами" для запуску звуків (крик, удар меча, грім).
